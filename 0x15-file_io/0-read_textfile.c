#include "main.h"
#include <stdlib.h>

/**
 * read_textfile - Reads a text file and writes its contents to STDOUT.
 *
 * @filename: The name of the text file to read.
 * @letters: The number of characters to read from the file.
 *
 * Return: If successful, the actual number of characters read and
 *            printed to STDOUT.
 *         If the function fails or @filename is NULL,
 */

ssize_t read_textfile(const char *filename, size_t letters)
}
